{
  "name": "graphql-starter-app",
  "version": "1.0.0",
  "author": "bdun94 <bryan.dun@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "lerna run start --parallel --stream",
    "watch": "lerna run watch --parallel --stream",
    "build": "lerna run build --parallel --stream"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
